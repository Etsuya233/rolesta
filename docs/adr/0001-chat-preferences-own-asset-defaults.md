# 聊天偏好集中持有资产默认引用

Rolesta 将默认 Persona、默认预设和默认模型连接作为当前用户的聊天偏好，由 `ChatPreferencesModule` 通过 `asset_defaults` 集中持有资产 ID。该方案取代在三类资产表分别增加 `is_default` 或在用户记录中保存 JSON 的做法，因为默认关系跨越多个资产上下文，且每种类型对每个用户最多只有一个；集中存储可以统一表达可空、替换和删除后置空的语义，也让角色卡、预设和模型连接聚合继续只描述资产本身。默认引用指向资产身份并跟随已保存内容变化，不承诺资产当前具备聊天运行条件。
